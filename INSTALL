"INSTALL" of LIBPOLYMNIA

1. cmakeを利用するやうに變更した。cmake, make, make install を順に實行すれば良い筈である。
2. 現状、CMakeLists.txt に手許の環境用の設定が含まれてゐるので、適宜變更されたい。
3. Autotools用の設定ファイルが as is で殘つてゐるので、そちらを適宜修正して利用することも可。恐らく無修正ではうまくいかない。
